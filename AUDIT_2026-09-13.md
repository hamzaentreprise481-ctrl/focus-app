# FOCUS verification — 13 September 2026

## Conclusion

FOCUS is not production-ready. The production domain still serves the initial
prototype. The existing teacher/public-site implementation is in unmerged PR #1.
This pass preserved that work and independently reproduced the deployment mismatch.

## Reproduced findings

- Production `/` and `/classes`: HTTP 200, original public demonstration dashboard.
- Production `/connexion` and `/app`: HTTP 404. There is no working teacher-login
  route on that domain. This explains why testing the PR's authentication code
  cannot establish that authentication works in production.
- GitHub `main`: `6a883010e4caca4d3417782a23be227561359515` (initial prototype).
- Existing PR #1 head before this pass: `1bb6aa1df0d964aa2481b6e0bc763858eccc7995`.
  GitHub workflow 34628876328 completed successfully. Its Vercel status is Ready
  for project `focus-app-nhkt`, deployment `DGRetY5W6ZtDkviLtdSbZw7YgB7h`.
  The Preview redirects to Vercel login; Ready is not application QA.
- Vercel team discovery returned an empty list. A direct request to the known
  FOCUS project/team returned 403 and explicitly required reauthentication to
  that scope. The browser also requires sign-in.
- The owner-designated Supabase project denies connector access. Its browser
  dashboard redirects to sign-in. Auth users, last sign-in, audit logs, env
  values, schema, migrations, RLS and database persistence remain unverified.
- The code uses Supabase only for Auth. Academic records are fixtures and
  browser-local additions; there are no school-data queries/migrations here.
- The security test's Unix-only path filter finds zero teacher pages on Windows.
  Reproduced as 20/21 tests passing, fixed with `path.basename`, then 21/21 passing.
- README deployment instructions described Preview-only variables and a stale
  no-production restriction, contradicting the current authorized task.

## Changes in this pass

- `tests/security.test.ts`: platform-independent teacher-page discovery.
- `tests/integration/routes.test.ts`: negative password/input checks, re-login
  after logout, external-return-URL rejection, deployment smoke verification.
- `scripts/check-deployment.mjs` and `package.json`: anonymous, read-only
  deployment checks that fail for stale routes or an inactive login form.
  Output deliberately excludes cookies, response bodies and provider payloads.
- `README.md`, `FOCUS_PRODUCT.md`: current deployment authorization, production
  and preview configuration, cookie/domain behavior and explicit evidence limits.
- This audit records current findings without overwriting the earlier report.

No application authorization was weakened, no real credentials were changed,
and no database/infrastructure was created, deleted or modified.

## Validation performed

- Clean lockfile install: 443 packages added, zero reported vulnerabilities.
- Existing production build: passed on Windows, Node 24.19.0.
- Typecheck and ESLint: passed again with the final added test cases and script.
- Unit/security tests: 21 passed after the Windows fix.
- Route tests: 14 passed against the production build and the existing isolated
  loopback Auth double. The actual Server Actions and route guards execute.
- Deployment smoke: production fails all 8 checks; unconfigured local build
  passes 7 and correctly fails enabled-login configuration; configured test
  server passes all 8 inside the route suite.
- Browser: local public homepage inspected at desktop, 390px mobile and 768px
  tablet widths. Mobile has no document horizontal overflow. Login CTA,
  return-to-site, FAQ expansion and anchor targets checked. Anonymous `/app`
  redirects to `/connexion?next=...`. With no local credentials the form is
  visibly disabled, as intended. No warnings/errors captured on these pages.
- Pattern scan over all fetched Git commits found no matches for long JWTs,
  `sb_secret_` keys or private-key headers. This is limited pattern coverage,
  not a certification of all secrets, external envs, or deployed bundles.

The bundled Windows sandbox cannot answer `os.userInfo()` for the tsx cache
directory. A workspace-only preload supplied a fallback username to run tests;
it is outside this repository, changes no assertions and is not a deployable
application dependency. An attempted separate browser Auth-fixture server was
denied by automatic approval review. Authenticated browser workflows were not
completed. HTTP test success does not prove real Supabase login.

## Remaining work, by severity

**BLOCKER:** Restore FOCUS Vercel/Supabase access; inspect production and Preview
variable scopes and the existing teacher account. Confirm the selected project,
branch, domain and backend agree. Diagnose any real provider failure using
redacted logs before editing configuration.

**BLOCKER:** Real teacher password login, refresh persistence, logout/re-login,
unauthorized-role denial and browser/network errors need end-to-end verification.
The production domain currently has no login route. No production promotion was
performed without the required provider/behavior verification.

**BLOCKER for real school use:** Inspect the existing schema first, then connect
academic workflows to properly authorized persistent storage, with resource
ownership, teacher/class membership and tested RLS. Do not enter real student
data in the current browser-only demonstration.

**IMPORTANT:** Authenticated evaluation creation/editing, storage errors,
student/class views, keyboard and responsive layouts still need real browser QA.
Interventions are suggestions only. Invitation/password recovery needs the
existing provider configuration and a tested user journey.

**IMPORTANT:** Vercel duplicates, production environment scopes, domain aliases,
Node/build settings and rollback targets remain unaudited due to access. Keep
existing deployments; do not delete projects speculatively.

**MINOR:** Demo requests have no configured destination by default; the site
states that no request is collected. Dependency install reports ESLint 9 as
unsupported; a tooling upgrade is separate from restoring the production flow.

## Production model and verification gate

Use the existing repository, `main`, the existing `focus-app-nhkt` Vercel
project and the owner's existing Supabase backend. The observed production URL
is https://focus-app-nhkt.vercel.app. Production branch settings and duplicate
project inventory still need confirmation in Vercel; do not infer them solely
from a successful GitHub status.

After provider access is restored, test the Preview with the real teacher,
promote through the existing Git integration, run `npm run check:deployment --
https://focus-app-nhkt.vercel.app`, then repeat the real teacher QA on production.
The current task already authorizes these actions; missing service access is
the obstacle. No unrelated project was inspected or changed.
